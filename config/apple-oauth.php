<?php

/**
 * Apple OAuth package configuration.
 *
 * Placeholder scaffold — configuration keys arrive with feature code
 * in later milestones (client credentials, token storage driver,
 * default scopes, etc.). Kept as an empty array so the package
 * registers a config namespace that consumer apps can already
 * publish and override.
 *
 * @package    ArtisanPack_UI
 * @subpackage AppleOAuth
 *
 * @since      1.0.0
 */

declare( strict_types=1 );

return [
    //
];
