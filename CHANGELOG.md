# ArtisanPack UI Apple OAuth Changelog

## [Unreleased]

### Added
- Initial package skeleton scaffolded from `artisanpack-ui/package-blueprint`: composer.json, service provider, facade, helpers, CI, code-style config, and test harness.
- README rewrite covering the OAuth flow, credential drivers, client-secret JWT, scope registry, and Apple Developer setup walkthrough (Services ID, `.p8` key, Team ID, Key ID, return URLs).
